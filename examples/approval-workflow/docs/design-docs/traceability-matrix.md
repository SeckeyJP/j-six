# トレーサビリティマトリクス — 申請承認ワークフロー

**対象タスク**: TASK-AW-002 ／ **生成日**: 2026-09-19 ／ **生成元**: 証跡パッケージ `reports/evidence/TASK-AW-002/01_traceability.md`
**生成方法**: `doc-reverse-gen` Skill 種別 `quality`。証跡の数値を引用し、再計算・推定をしていない

> 本書は証跡パッケージを整形したビューであり、直接編集しない。

---

## 1. 証跡（決定論的な検証結果）

> 出典: `reports/evidence/TASK-AW-002/01_traceability.md`

| タスク | 判定 | 要件・性質の総数 | テストが存在するもの | 未トレース | JUnit のテスト名も走査 |
|---|---|---|---|---|---|
| TASK-AW-002 | ✅ 合格 — traceability: 全21件トレース済（REQ-\d+: 12件 / PROP-\d+: 9件） | 21 | 21 | 0 | はい |

| ID パターン | 定義数 | トレース済 | 未トレース |
|---|---|---|---|
| `REQ-\d+` | 12 | 12 | なし |
| `PROP-\d+` | 9 | 9 | なし |

証跡が保証しているのは「定義された21件のすべてに、ID を含むテストが存在する」ことである。
どのテストがどの要件に対応するかの一覧は証跡パッケージに含まれない。

## 2. 要件 ⇔ テスト ⇔ 実装 ⇔ ADR の対応（参照）

要件ごとの対応表（REQ-001〜012、PROP-001〜009、hold-out 受入テスト ⇔ ユースケース）は
`docs/traceability.md`（`j-six:traceability` Skill の出力に相当）にあり、
[基本設計書 11 トレーサビリティ](kihon-sekkei.md#11-トレーサビリティ)に収めている。
同じ表を2冊に載せないため、本書では参照だけにする。

同表のテスト件数（unit 81 + hold-out 72 = 153 tests）は、[テスト結果報告書](test-report.md) の
証跡（通常 81件・hold-out 72件）と一致する。

---

## 3. 参考所見（AI による所見）

> ⚠ **AI による参考所見。単独で品質判定の根拠にしない。** 決定論的な検証結果（証跡）ではない。

本書の元データ（`01`）に対応する参考所見は無い。

---

## 4. 承認

**未承認。** `07_approval.md` の確認項目・承認欄はすべて未記入である。詳細は[レビュー記録](review-record.md)を参照。

---

## 検証条件

> 出典: `reports/evidence/TASK-AW-002/env.json`

| 項目 | 値 |
|---|---|
| 対象 commit | `f3ccb671a321c6d96b446ff9d3679a35ae4a92d5` |
| 基準点（RED タグ） | `jsix/red-TASK-AW-002` |
| 証跡の生成日時 | 2026-09-19T05:03:09.803547+00:00 |
| ゲート設定 | `.jsix-checks.json`（SHA-256 `c58f8dd3f9273a9286f3af53256164470c1ce4e3951a8ac1ba03621e4240517d`） |
| 実行環境 | Python 3.9.6 / macOS-26.6.2-arm64-arm-64bit |
| ツール（`env.json` に記録されたもの） | Bandit 1.8.6 |

**証跡生成後の変更**: なし。証跡は本書の生成時点の HEAD `f3ccb67` に対して生成された。本書を含む
`docs/deliverables/`・`docs/design-docs/` は未コミットの作業ツリーとして、スコープ検査（変更 27ファイル）の対象に含まれている。
