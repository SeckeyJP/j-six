# セキュリティ診断結果 — 申請承認ワークフロー

**対象タスク**: TASK-AW-002 ／ **生成日**: 2026-09-19 ／ **生成元**: 証跡パッケージ `reports/evidence/TASK-AW-002/04_security.md`
**生成方法**: `doc-reverse-gen` Skill 種別 `quality`。証跡の数値を引用し、再計算・推定をしていない

> 本書は証跡パッケージを整形したビューであり、直接編集しない。

---

## 1. 証跡（決定論的な検証結果）

> 出典: `reports/evidence/TASK-AW-002/04_security.md`（SARIF の集計。解析ツールは利用者側が選ぶ）

| タスク | 診断 | 判定 | ツール | error | warning | note | 抑止済み | 閾値 |
|---|---|---|---|---|---|---|---|---|
| TASK-AW-002 | 静的解析（SAST） | ✅ 合格（error 以上の指摘なし） | Bandit 1.8.6 | 0 | 0 | 0 | 0 | error |
| TASK-AW-002 | 秘密情報スキャン | ⏭ **未実施**（reports/secrets.sarif が無いためスキップ。optional 指定。CI で生成される想定） | — | — | — | — | — | 未設定 |
| TASK-AW-002 | 依存脆弱性 | ⏭ **未実施**（reports/deps.sarif が無いためスキップ。optional 指定。CI で生成される想定） | — | — | — | — | — | 未設定 |

**秘密情報スキャンと依存脆弱性は実施されていない。** 証跡の表には件数 0 と記録されているが、
SARIF が無くスキップされたことによる値であり、「指摘が0件だった」ことを意味しない。本書では「—」とした。

### 診断の範囲外

次の事項は本診断の対象ではない。設計上の扱いは[基本設計書](kihon-sekkei.md) 9 章を参照。

- 認証・認可（要求 Spec 2.2 で対象外。actor の ID は既存 SSO が担保する前提。要求 Spec 5）
- 動的診断（DAST）・ペネトレーションテスト（証跡パッケージに該当する記録が無い）

---

## 2. 参考所見（AI による所見）

> ⚠ **AI による参考所見。単独で品質判定の根拠にしない。** 決定論的な検証結果（証跡）ではない。

本書の元データ（`04`）に対応する参考所見は無い。証跡パッケージの要約節（`00_summary.md`）は**未記入**である。

---

## 3. 承認

**未承認。** `07_approval.md` の確認項目・承認欄はすべて未記入である。詳細は[レビュー記録](review-record.md)を参照。

---

## 検証条件

> 出典: `reports/evidence/TASK-AW-002/env.json`

| 項目 | 値 |
|---|---|
| 対象 commit | `f3ccb671a321c6d96b446ff9d3679a35ae4a92d5` |
| 基準点（RED タグ） | `jsix/red-TASK-AW-002` |
| 証跡の生成日時 | 2026-09-19T05:03:09.803547+00:00 |
| ゲート設定 | `.jsix-checks.json`（SHA-256 `c58f8dd3f9273a9286f3af53256164470c1ce4e3951a8ac1ba03621e4240517d`） |
| 実行環境 | Python 3.9.6 / macOS-26.6.2-arm64-arm-64bit |
| ツール（`env.json` に記録されたもの） | Bandit 1.8.6 |

**証跡生成後の変更**: なし。証跡は本書の生成時点の HEAD `f3ccb67` に対して生成された。本書を含む
`docs/deliverables/`・`docs/design-docs/` は未コミットの作業ツリーとして、スコープ検査（変更 27ファイル）の対象に含まれている。
