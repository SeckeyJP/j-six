---
type: tool_used
tool: Read
input_match: "spec-create/templates/requirement-spec\\.md"
arm: with-only
---

Skill に同梱したテンプレート（`${CLAUDE_SKILL_DIR}/templates/requirement-spec.md`）を読んだこと。
リポジトリルートの `templates/` を参照していた頃は、インストール先に存在せず失敗していた。
